{
  "version": 2,
  "builds": [
    {
      "src": "package.json",
      "use": "@vercel/next"
    }
  ],
  "installCommand": "npm install && npm install --workspace apps/web --workspace apps/api",
  "buildCommand": "npm run build --workspace apps/web",
  "outputDirectory": "apps/web/.next"
}
